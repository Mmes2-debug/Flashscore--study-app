{
  "name": "magajico-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/backend && npm run build --workspace=apps/frontend",
    "tsc:check": "npm run type-check --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "test:all": "npm test && npm run test:e2e",
    "start:all": "concurrently \"npm run start:backend\" \"npm run start:ml\" \"npm run start:frontend\"",
    "start:backend": "cd apps/backend && node dist/main.js",
    "start:ml": "cd apps/backend/ml && uv run python api.py",
    "start:frontend": "cd apps/frontend && npm start",
    "build:all": "npm run build --workspace=packages/shared && npm run build --workspace=@magajico/backend && cd apps/frontend && npm run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@next-auth/mongodb-adapter": "^1.1.3",
    "@types/bcryptjs": "^2.4.6",
    "bcryptjs": "^3.0.2",
    "concurrently": "^9.2.1",
    "critters": "^0.0.23",
    "dotenv": "^17.2.3"
  }
}
